#include<stdio.h>
#include"main.h"
/**
 *mul - multiply two integers and returns the result.
 *@a: The intgr to mul
 *@b: The intgr to mul
 *
 * Return: On success z.
 */
int mul(int a, int b)
{
int z = a * b;
return (z);
}
