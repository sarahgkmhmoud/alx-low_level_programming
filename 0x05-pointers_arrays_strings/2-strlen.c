#include<stdio.h>
#include"main.h"
/**
 * _strlen - writes the character c to stdout
 * @s : Decribe the char arry
 * Return: On success i.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
		return (i);
}
