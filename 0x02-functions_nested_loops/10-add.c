#include<stdio.h>
#include"main.h"
/**
 *add - adds two integers and returns the result.
 *@x: The intgr to add
 *@y: The intgr to add
 *
 * Return: On success z.
 */
int add(int x, int y)
{
int z = x + y;
return (z);
}
